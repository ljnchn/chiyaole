<!--pages/medication/list.wxml-->
<view class="container">
  <view class="stats-section">
    <view class="stat-card">
      <text class="stat-label">药品总数</text>
      <view class="stat-value">
        <text class="stat-number">{{stats.total}}</text>
        <text class="stat-unit">项</text>
      </view>
    </view>
    <view class="stat-card warning">
      <text class="stat-label">库存告急</text>
      <view class="stat-value">
        <text class="stat-number">{{stats.lowStock}}</text>
        <text class="stat-unit">项</text>
      </view>
    </view>
  </view>

  <!-- 空态 -->
  <view wx:if="{{medications.length === 0}}" class="empty-state">
    <t-icon name="shop" size="96rpx" color="#c4c4c9" />
    <text class="empty-title">还没有添加药品</text>
    <text class="empty-desc">点击右下角按钮添加您的药品</text>
  </view>

  <view wx:else class="medication-grid">
    <view
      wx:for="{{medications}}"
      wx:key="id"
      class="medication-card"
      bindtap="onMedicationTap"
      data-id="{{item.id}}"
    >
      <view class="card-image">
        <view class="placeholder-image" style="background-color: {{item.color}}40;">
          <text class="avatar-text">{{item.avatarText}}</text>
        </view>
        <view wx:if="{{item.lowStock}}" class="low-stock-badge">
          <text>库存告急</text>
        </view>
      </view>
      <view class="card-content">
        <text class="medication-name">{{item.name}}</text>
        <text class="medication-spec">{{item.specification}}</text>
        <view class="stock-info">
          <text class="stock-text {{item.lowStock ? 'warning' : ''}}">
            剩余 {{item.remaining}}/{{item.total}} {{item.unit}}
          </text>
          <view class="stock-progress">
            <view
              class="progress-bar"
              style="width: {{item.stockPercent}}%; background-color: {{item.lowStock ? '#e53935' : item.color}};"
            />
          </view>
        </view>
      </view>
    </view>
  </view>

  <view class="fab" bindtap="onAddMedication">
    <t-icon name="add" size="48rpx" color="#ffffff" />
  </view>
</view>